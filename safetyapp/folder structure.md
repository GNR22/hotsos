# 📂 hotsos (Main Project Root)

This is your master folder. It contains two distinct sub-projects: the Mobile App (Sender) and the Web Dashboard (Receiver).

---

## 1. 📱 safetyapp/ (The Mobile App)

**Technology:** React Native (Expo) + Supabase  
**Purpose:** The Android app that sends the SOS signal.  
**Action:** Run your terminal commands (`npx expo start`, `eas build`) inside this folder.

```text
hotsos/
├── safetyapp/
│   ├── .expo/                  # (Auto-generated by Expo)
│   ├── assets/                 # App icons and splash screens
│   ├── components/             # Custom UI logic
│   │   ├── SOSButton.js        # [CRITICAL] Main logic for long-press & SMS trigger
│   │   └── EmergencyTracker.js # Handles live GPS updates to Supabase
│   ├── lib/                    # Configuration files
│   │   └── supabase.js         # API Keys & Database connection setup
│   ├── utils/                  # Helper functions
│   │   └── getAddress.js       # Converts GPS coords to street names (Reverse Geocoding)
│   ├── App.js                  # Main entry point (Login/Auth logic)
│   ├── app.json                # [CRITICAL] Config for Android Permissions (SEND_SMS)
│   ├── eas.json                # Build profile for making the APK
│   ├── package.json            # List of installed libraries
│   └── babel.config.js         # Compiler settings



hotsos/
├── SafeWalk_Web/
│   ├── index.html              # [CRITICAL] The Map Viewer & Supabase Listener
│   ├── style.css               # (Optional) Styles for the map overlay
│   └── script.js               # (Optional) Separated Logic for the map
```
